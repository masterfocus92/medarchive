# /etc/postgresql/16/main/conf.d/medarchive.conf
# Тюнинг под VPS 1 ГБ RAM (ADR-017): PG делит машину с двумя uvicorn,
# Caddy и второй PWA владельца — умеренность важнее скорости.
shared_buffers = 128MB
max_connections = 30
work_mem = 8MB
maintenance_work_mem = 32MB
effective_cache_size = 256MB
